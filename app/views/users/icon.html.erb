<table>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.post %></td>
        <td><%= image_tag(post.image.url) %></td>
        <td><%= post.user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to '投稿一覧', posts_path %>
