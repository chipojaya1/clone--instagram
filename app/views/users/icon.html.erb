<table>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.posts %></td>
        <td><%= image_tag(post.image.url) %></td>
        <td><%= post.user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Home', posts_path %>
