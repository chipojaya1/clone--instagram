<h1><%= @user.name %>User</h1>
<p>Email address: <%= @user.email %></p>
<% if @user.icon.present? %>
  <%= image_tag(@user.icon.url) %>
<% else %>
    <%= image_tag("no_image.jpeg") %>
<% end %>
<button type="button" class="btn btn-default"><%= link_to 'Edit', edit_user_url %>
